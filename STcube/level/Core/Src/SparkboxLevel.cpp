#include "SparkboxLevel.h"

SparkboxLevel::SparkboxLevel(string levelPath)
{
	// Initialize audio manager
	//audio = SparkboxAudioManager::getInstance();
	//audio->importAllAudioFiles(levelPath);
	// Add a new audio thread
}
